## 1. Rename fields and parameters

- [x] 1.1 Rename `jogoGame` to `app` in `GameplayScreen.java`
- [x] 1.2 Rename `jogoGame` to `app` in `MainMenuScreen.java`
- [x] 1.3 Rename `jogoGame` to `app` in `QuizScreen.java`
- [x] 1.4 Rename `jogoGame` to `app` in `GameOverScreen.java`
- [x] 1.5 Rename `jogoGame` to `app` in `VictoryScreen.java`

## 2. Verify

- [x] 2.1 Build project with `gradlew :core:compileJava`
